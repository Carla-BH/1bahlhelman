import cart from "./Asset/Carrito fondo negrp (2).png"

const CartWidget = () => {
    return(
        <div>
            <img src={cart} alt="cart-widget"/>
            0
        </div>
    )
}

export default CartWidget